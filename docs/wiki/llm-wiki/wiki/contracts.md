# Contratos REST

## HECHO

El PRD exige REST/JSON directo entre `citas-web` y `citas-api`.

## PREGUNTA ABIERTA

Todavía no existe un contrato aprobado de endpoints, DTOs, errores, paginación, versionado, refresh/logout, disponibilidad, reservas ni webhook n8n. No inferirlo desde nombres de pantallas.
